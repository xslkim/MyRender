>>> 上集回顾 · 本集目标 #B01
@enter: fade-up
@exit: fade
@visual: image(../assets/albedo.png)

--- visual ---
（实际使用 ../assets/albedo.png：只有材质基础色、没有任何光照的画面，颜色平、扁，像素已成形但"素颜"。）

--- narration ---
上一集结束时
三角形已经变成了像素
但只有形状
这就是它"素颜"的样子
只有材质的基础色
没有光
这一集
我们把光打上去
讲清楚一个像素的最终颜色
到底是怎么算出来的


>>> 材质的两张关键贴图 #B02
@enter: fade
@exit: fade
@visual: animation

--- visual ---
全屏深色背景 (#0d1117)。左右两块，每块占画布 44% 宽，圆角 14px，背景 #161b22，边框 1px solid #30363d，内部上方放一张示意缩略图、下方一行标签。
  左：标签 "基础色贴图 (albedo)" 32px 白色，缩略图用 ../assets/albedo.png；下方 24px #8b949e "这块表面本来是什么颜色"
  右：标签 "法线贴图 (normal)" 32px 白色，缩略图用 ../assets/normal_mapped.png；下方 24px #8b949e "表面凹凸的朝向细节"
[0s] 两块依次淡入，间隔 0.5s。
[1.5s] 中间出现一个加号和等号，示意"基础色 + 朝向 = 有立体感的表面"。

--- narration ---
一个 URP 材质
最核心的就两样输入
一张 **基础色贴图**
告诉你这块表面本来是什么颜色
一张 **法线贴图**
记录表面细小的凹凸朝向
有了朝向
平的一块墙
也能显出砖缝和颗粒的立体感


>>> 法线：表面朝哪边 #B03
@enter: fade-up
@exit: fade
@visual: image(../assets/normal_mapped.png)

--- visual ---
（实际使用 ../assets/normal_mapped.png：把每个像素的表面朝向编码成颜色显示，墙面、地面、工具表面呈现不同的法线色块和细节。）

--- narration ---
这张图
把每个像素"朝哪边"
用颜色画了出来
不同颜色代表不同朝向
为什么朝向这么重要
因为光照的第一条规则就是
**正对着光的面亮，侧着的面暗**
所以算颜色之前
必须先知道每个像素朝哪
法线贴图就是干这个的


>>> 直接光：太阳和射灯 #B04
@enter: fade
@exit: fade
@visual: image(../assets/direct_only.png)

--- visual ---
（实际使用 ../assets/direct_only.png：只保留直接光、关掉环境光的画面——受光面明亮偏暖，背光面很暗甚至发黑，对比强烈。）

--- narration ---
这是只留 **直接光** 的样子
场景里有两盏
一盏是平行光，相当于太阳
一盏是工作台上方的射灯
面对着光的地方亮
背着光的地方很暗
金属和粗糙表面的反光也不一样
这一整套
就是大家说的 **PBR**
基于物理的光照
但你不用记公式
记住"朝向决定明暗"就够了


>>> 但背光的地方为什么不是全黑 #B05
@enter: fade-up
@exit: fade
@visual: image(../assets/ambient_only.png)

--- visual ---
（实际使用 ../assets/ambient_only.png：只保留环境光的画面——整体柔和、偏冷蓝，没有硬阴影，背光处也有亮度，青色墙泛蓝。）

--- narration ---
回头看刚才那张
背光面虽然暗
却不是死黑
因为现实里
天空本身就是一个大光源
四面八方都有微光漫射过来
这就是 **环境光**
这张图就是只留环境光的样子
柔和、偏蓝
正是天空的颜色
注意那面青色的墙
被天空照得泛蓝


>>> 环境光怎么存：球谐 #B06
@enter: fade
@exit: fade
@visual: image(../assets/ambient_sh.png)

--- visual ---
（实际使用 ../assets/ambient_sh.png：环境光（球谐）单独可视化，整个场景被一层来自天空的柔光笼罩。）

--- narration ---
天空各个方向的光
颜色还不一样
顶上偏蓝、地平线偏白
要把"每个方向有多少光"存下来
直接存一整张全景图太浪费
于是用一种很省的方式
叫 **球谐**
只用二十七个数字
就能描述整片天空的光照分布
导出时
我们就把它从 Unity 抓出来
写进了 JSON


>>> 一个真实的 bug：环境光暗了三倍 #B07
@enter: fade-up
@exit: fade
@visual: animation

--- visual ---
全屏深色背景 (#0d1117)。横向三格对比，总宽占画布 94%，每格上方一个标签 28px，格内放对应截图（统一裁剪到青色墙区域）：
  ① 标签 "灭灯：纯黑" —— 用 ../assets/lights_off.png（裁到青墙区域，几乎全黑）
  ② 标签 "有 bug：墙发黑" 红色 (#ff7b72) —— 用 ../assets/cyan_bug_crop.png
  ③ 标签 "修复后：恢复青色" accent 色 —— 用 ../assets/cyan_fixed_crop.png
[0s] 三格依次淡入，间隔 0.6s。
[2s] 在 ② 和 ③ 之间画一个 accent 箭头，旁注 24px #8b949e："同一个亮度系数，被错误地除了两次。"

--- narration ---
讲个我真实踩到的坑
那面青色的墙是背光的
全靠环境光照亮
可一开始
它发黑、发灰
青色都快没了
在 CPU 上排查很方便
我直接打印中间值
发现一个用来归一化的亮度系数
被同时用在了直接光和环境光上
直接光那边有别的地方补偿回来了
环境光这边没有
等于平白被除暗了大概三倍
把它从环境光的路径上拿掉
青色墙立刻就回来了
这种 bug
在 GPU 上你只能干瞪眼


>>> 合成：基础色 × 光 #B08
@enter: fade
@exit: fade
@visual: image(../assets/final.png)

--- visual ---
（实际使用 ../assets/final.png：最终上色后的完整画面，直接光 + 环境光 + 材质全部合成。）

--- narration ---
把这些加起来
基础色
乘上直接光加环境光
再考虑金属度和粗糙度
就是每个像素的最终颜色
这张
就是合成完的结果
受光、背光、反光、天空的补光
全都对上了


>>> 本集小结 · 下集预告 #B09
@enter: fade-up
@exit: fade
@visual: animation

--- visual ---
全屏深色背景 (#0d1117)，垂直居中。
[0s] 顶部标题 "EP3 小结" 字号 60px 粗体 #e6edf3。
[0.5s] 下方四行要点，每行前带 accent 色 ✓，字号 32px，行距 24px，左对齐居中块（块宽占画布 76%）：
  ✓ 材质 = 基础色 + 法线（朝向）
  ✓ 直接光：朝向决定明暗（PBR）
  ✓ 环境光：天空的补光，用球谐存
  ✓ 在 CPU 上能打印中间值，揪出了环境光变暗的 bug
[2.5s] 底部一张半透明 ../assets/shadow_off.png 缩略图（宽 40% 画布，圆角 12px，opacity 0.5），上方叠加 accent 色文字 40px："下一集 → 阴影"。

--- narration ---
这一集
像素有颜色了
材质是基础色加法线
直接光看朝向
环境光是天空的补光
我们还在 CPU 上
靠打印中间值
抓出了环境光变暗的 bug
但你可能已经发现
现在的画面少了点什么
没有阴影
下一集
我们就把影子加上
我们下集见
